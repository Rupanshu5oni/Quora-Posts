<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <style>
        /* General Page Styling */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f4f4f9;
    margin: 0;
    padding: 20px;
    color: #333;
    line-height: 1.6;
}

/* Header Styling */
h2 {
    color: #800000; /* Dark maroon */
    text-align: center;
    margin-bottom: 20px;
    font-size: 2rem;
}

/* Post Information */
p {
    font-size: 1rem;
    margin: 10px 0;
    color: #555;
}

/* Form Styling */
form {
    max-width: 500px;
    margin: 0 auto;
    padding: 20px;
    background-color: #fff;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

/* Textarea Styling */
textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 1rem;
    margin-bottom: 15px;
    font-family: 'Arial', sans-serif;
    box-sizing: border-box;
}

/* Button Styling */
button {
    background-color: #800000;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
    transition: background-color 0.3s ease;
    display: block;
    width: 100%;
}

button:hover {
    background-color: #a52a2a;
}

/* Responsive Design */
@media (max-width: 768px) {
    form {
        padding: 15px;
    }

    textarea {
        font-size: 0.9rem;
    }

    h2 {
        font-size: 1.5rem;
    }

    button {
        font-size: 0.9rem;
        padding: 8px 16px;
    }
}

    </style>
</head>
<body>
    <h2>Edit your post</h2>
    <p>Post id : <%= post.id %></p>
    <p>Post username :  @<%= post.username %></p>
    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <textarea rows="10" cols="32" name="content"> <%= post.content %></textarea>
        <button>Submit</button>
    </form>
</body>
</html>